# 政治のトリセツ — デザイン方針

2026-09-16 改修。提供された Jeton のスタイルリファレンスを、愛知の政治情報を読むための UI に適用する。

## 見た目

- 白いキャンバス、暖色のインク `#360802`、広い余白を基本とする。
- ブランドの装飾は `#f73b20`。小さい文字・操作ボタンにはコントラストを確保した `#c3311b` を使う。
- 淡いピンク `#fdedea` は地域検索、淡いライム `#f5ffbb` は学習の入口に使用。政党や候補者の分類には使わない。
- カード・入力欄は16px、主要CTA・ナビはピル型。影は控えめにする。
- 見出し・本文は共通のシステムサンセリフ。外部フォント通信を不要にし、和文の本文は1.8程度の行間を確保する。
- 本文幅は最大1200px。デスクトップは2〜3列、スマートフォンは1列中心。主要タップ領域は44px以上。

## 情報の扱い

- 議員は同じカード、同じ情報項目で掲載する。所属に応じた色・大きさ・写真の差を設けない。
- 議員一覧はふりがな（未登録時は氏名）、氏名、IDで安定ソートし、そのルールを明示する。
- テーマは定義順に同じサイズで表示し、発言件数ランキングとして扱わない。
- トップの事実カードは公開日が新しい順。表示順を説明する。
- データの基準日、件数が活動評価ではないこと、元資料への導線を維持する。
- グラフは色だけに依存しない。投票率の2系列は実線・丸と破線・四角で区別し、数表を併設する。

## 詳細ページへの展開（2026-09-25）

- 見出しの上の小見出しは**日本語**で、アクセント色（`text-sm font-medium tracking-wider text-accent`）。英語ラベル（Learn、Methodology など）は使わない。
- 区切り線は細い線（`border-line`）に統一。太い黒線（`border-b-[3px] border-ink`）は使わない。
- 入力欄の枠線は `--color-field`（白地に対し3:1以上）。
- 時期で変わる説明には「〇月〇日時点」を付ける（争点解説は `nowAsOf`）。
- 図解・早見表（`WhoYouChoose`・`WhoDoesWhat`）は、各行の根拠を条文やデータで示し、色で意味づけしない。

## 実装

- 共通トークン・既存詳細画面への適用：`app/globals.css`
- ヘッダ・モバイルメニュー：`components/SiteHeader.tsx`
- 下部ナビ：`components/BottomNav.tsx`
- トップ：`app/page.tsx`、`components/CivicIllustration.tsx`、`components/TopicIcon.tsx`
- 地域・議員・テーマ・争点の既存検索とページ遷移を維持。掲載データと出典は変更しない。
- キーボードフォーカス、入力ラベル、エラー通知、選択状態、動きの抑制を用意する。

## 検証

- データ検証、ESLint、TypeScript、本番静的生成。
- ブラウザ：デスクトップ、390px、320px。
- 郵便番号：対象外の番号でエラー、460-0008で中区へ遷移。
- 議員：該当なしの表示、条件解除、県議会97名の絞り込み、50名から97名への追加表示。
